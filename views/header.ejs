<!DOCTYPE html>
<html>
<head>
  <title><%= title %> - Lịch Cúp Điện</title>
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.7.2/font/bootstrap-icons.css" rel="stylesheet">
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
  
  <!-- Styles chung -->
  <style>
    :root {
      --primary-color: #0d6efd;
      --secondary-color: #6c757d;
    }

    body {
      background-color: #f8f9fa;
    }

    .navbar {
      background: linear-gradient(135deg, var(--primary-color), #0143a3);
      box-shadow: 0 2px 4px rgba(0,0,0,.1);
    }

    .card {
      border: none;
      box-shadow: 0 2px 4px rgba(0,0,0,.05);
      transition: transform 0.2s;
      border-radius: 12px;
      overflow: hidden;
    }

    .card:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0,0,0,.1);
    }

    .card-header {
      background: white;
      border-bottom: 2px solid #f0f0f0;
    }

    .btn {
      border-radius: 6px;
      padding: 8px 16px;
    }

    .btn-primary {
      background: linear-gradient(135deg, var(--primary-color), #0143a3);
      border: none;
    }

    .company-select {
      max-height: 400px;
      overflow-y: auto;
      border-radius: 8px;
    }

    .company-item {
      padding: 12px 16px;
      border-bottom: 1px solid #eee;
      transition: all 0.2s;
    }

    .company-item:hover {
      background-color: #f8f9fa;
      transform: translateX(4px);
    }

    .company-item.selected {
      background-color: #e7f1ff;
      border-left: 4px solid var(--primary-color);
    }

    .alert {
      border: none;
      border-radius: 8px;
    }

    .table {
      background: white;
      border-radius: 8px;
      overflow: hidden;
    }

    .modal-content {
      border: none;
      border-radius: 12px;
    }

    .badge {
      padding: 6px 12px;
      border-radius: 6px;
    }

    /* Hero section styles */
    .hero-title {
      font-size: 3.5rem;
      font-weight: 700;
      line-height: 1.2;
    }
    
    .gradient-text {
      background: linear-gradient(135deg, var(--primary-color), #0143a3);
      -webkit-background-clip: text;
      -webkit-text-fill-color: transparent;
    }
    
    .hero-illustration {
      position: relative;
      height: 400px;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    
    .hero-icon {
      position: relative;
      z-index: 2;
      font-size: 6rem;
      color: var(--primary-color);
      animation: float 3s ease-in-out infinite;
    }
    
    .hero-circles .circle {
      position: absolute;
      border-radius: 50%;
      opacity: 0.1;
    }
    
    .circle-1 {
      width: 300px;
      height: 300px;
      background: var(--primary-color);
      animation: pulse 2s ease-in-out infinite;
    }
    
    .circle-2 {
      width: 200px;
      height: 200px;
      background: var(--primary-color);
      animation: pulse 2s ease-in-out infinite 0.3s;
    }
    
    .circle-3 {
      width: 100px;
      height: 100px;
      background: var(--primary-color);
      animation: pulse 2s ease-in-out infinite 0.6s;
    }
    
    @keyframes float {
      0% { transform: translateY(0px); }
      50% { transform: translateY(-20px); }
      100% { transform: translateY(0px); }
    }
    
    @keyframes pulse {
      0% { transform: scale(1); opacity: 0.1; }
      50% { transform: scale(1.1); opacity: 0.2; }
      100% { transform: scale(1); opacity: 0.1; }
    }
    
    .lead i {
      margin-right: 8px;
    }
    
    /* Responsive adjustments */
    @media (max-width: 768px) {
      .hero-title {
        font-size: 2.5rem;
      }
      
      .hero-illustration {
        height: 300px;
      }
      
      .hero-icon {
        font-size: 4rem;
      }
      
      .circle-1 { width: 200px; height: 200px; }
      .circle-2 { width: 150px; height: 150px; }
      .circle-3 { width: 80px; height: 80px; }
    }

    /* Header styles */
    .top-header {
      background: linear-gradient(135deg, var(--primary-color), #0143a3);
      padding: 0.5rem 0;
      color: white;
    }
    
    @media (max-width: 768px) {
      .top-header {
        display: none; /* Ẩn top header trên mobile để tiết kiệm không gian */
      }
    }
    
    .top-header a {
      color: rgba(255,255,255,0.9);
      text-decoration: none;
      font-size: 0.9rem;
    }
    
    .main-header {
      background: white;
      padding: 1rem 0;
      box-shadow: 0 2px 4px rgba(0,0,0,.05);
    }
    
    .main-header .container {
      position: relative;
    }
    
    .mobile-menu-toggle {
      display: none;
    }
    
    @media (max-width: 768px) {
      .mobile-menu-toggle {
        display: block;
        border: none;
        background: none;
        color: var(--primary-color);
        font-size: 1.5rem;
        padding: 0.5rem;
      }
      
      .main-nav {
        display: none;
        position: absolute;
        top: 100%;
        left: 0;
        right: 0;
        background: white;
        padding: 1rem;
        box-shadow: 0 4px 6px rgba(0,0,0,.1);
        border-radius: 0 0 12px 12px;
      }
      
      .main-nav.show {
        display: block;
      }
      
      .nav {
        flex-direction: column;
        gap: 0.5rem;
      }
      
      .nav-item {
        width: 100%;
      }
      
      .nav-link {
        width: 100%;
        text-align: left;
        padding: 0.75rem 1rem !important;
      }
    }
    
    .nav-link {
      font-weight: 500;
      padding: 0.5rem 1rem !important;
      border-radius: 6px;
      transition: all 0.2s;
    }
    
    .nav-link:hover {
      background: rgba(13, 110, 253, 0.1);
      color: var(--primary-color);
    }
    
    .nav-link.active {
      background: var(--primary-color);
      color: white !important;
    }

    /* Card styles */
    .card {
      border: none;
      box-shadow: 0 2px 4px rgba(0,0,0,.05);
      transition: transform 0.2s;
      border-radius: 12px;
      overflow: hidden;
    }
    
    .card:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 8px rgba(0,0,0,.1);
    }
    
    .card-body {
      padding: 1.5rem;
    }
    
    .card-title {
      font-size: 1.1rem;
      font-weight: 600;
      margin-bottom: 1rem;
      color: #2d3436;
    }
    
    .card-text {
      font-size: 0.95rem;
      margin-bottom: 0.75rem;
    }
    
    .card-text i {
      width: 20px;
      color: var(--primary-color);
      margin-right: 0.5rem;
    }
    
    .card-action {
      margin-top: 1.25rem;
      padding-top: 1.25rem;
      border-top: 1px solid #f0f0f0;
    }
    
    @media (max-width: 768px) {
      .card {
        margin-bottom: 1rem;
      }
      
      .card-body {
        padding: 1.25rem;
      }
      
      .row-cols-1 {
        margin-left: -0.5rem;
        margin-right: -0.5rem;
      }
      
      .row-cols-1 > .col {
        padding-left: 0.5rem;
        padding-right: 0.5rem;
      }
      
      .card-action .btn {
        width: 100%;
        padding: 0.75rem;
        display: flex;
        align-items: center;
        justify-content: center;
        gap: 0.5rem;
      }
      
      .card-action .btn i {
        font-size: 1.1rem;
      }
    }
  </style>

  <!-- Styles tùy chỉnh -->
  <% if (locals.customStyles) { %>
    <style>
      <%- customStyles %>
    </style>
  <% } %>
</head>
<body>
  <!-- Top Header -->
  <div class="top-header">
    <div class="container">
      <div class="d-flex justify-content-between align-items-center">
        <div>
          <a href="tel:19001909" class="me-3">
            <i class="bi bi-telephone"></i> 1900 1909
          </a>
          <a href="mailto:support@lichcupdien.com">
            <i class="bi bi-envelope"></i> support@lichcupdien.com
          </a>
        </div>
        <div>
          <% if (locals.user) { %>
            <div class="dropdown d-inline-block">
              <a class="dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown">
                <% if (user.picture) { %>
                  <img src="<%= user.picture %>" alt="Avatar" class="rounded-circle me-1" width="24" height="24">
                <% } %>
                <%= user.name %>
              </a>
              <ul class="dropdown-menu dropdown-menu-end">
                <li><a class="dropdown-item" href="/subscriptions"><i class="bi bi-gear"></i> Quản lý đăng ký</a></li>
                <li><hr class="dropdown-divider"></li>
                <li><a class="dropdown-item" href="/auth/logout"><i class="bi bi-box-arrow-right"></i> Đăng xuất</a></li>
              </ul>
            </div>
          <% } else { %>
            <a href="/login">
              <i class="bi bi-google"></i> Đăng nhập với Google
            </a>
          <% } %>
        </div>
      </div>
    </div>
  </div>

  <!-- Main Header -->
  <header class="main-header">
    <div class="container">
      <div class="d-flex justify-content-between align-items-center">
        <a class="navbar-brand d-flex align-items-center" href="/">
          <i class="bi bi-lightning-charge-fill me-2"></i>
          <span class="fw-bold">Lịch Cúp Điện</span>
        </a>
        <button class="mobile-menu-toggle d-md-none" type="button" onclick="toggleMobileMenu()">
          <i class="bi bi-list"></i>
        </button>
        <nav class="main-nav d-md-block">
          <ul class="nav">
            <li class="nav-item">
              <a class="nav-link <%= title === 'Trang chủ' ? 'active' : '' %>" href="/">
                <i class="bi bi-house"></i> Trang chủ
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link <%= title === 'Lịch Cúp Điện' ? 'active' : '' %>" href="/lich-cup-dien">
                <i class="bi bi-calendar-event"></i> Lịch cúp điện
              </a>
            </li>
            <li class="nav-item">
              <a class="nav-link" href="/api">API</a>
            </li>
            <% if (locals.user) { %>
              <li class="nav-item">
                <a class="nav-link <%= title === 'Quản lý Đăng ký' ? 'active' : '' %>" href="/subscriptions">
                  <i class="bi bi-bell"></i> Đăng ký thông báo
                </a>
              </li>
              <li class="nav-item d-md-none">
                <a class="nav-link" href="/auth/logout">
                  <i class="bi bi-box-arrow-right"></i> Đăng xuất
                </a>
              </li>
            <% } else { %>
              <li class="nav-item d-md-none">
                <a class="nav-link" href="/login">
                  <i class="bi bi-google"></i> Đăng nhập với Google
                </a>
              </li>
            <% } %>
          </ul>
        </nav>
      </div>
    </div>
  </header>

  <script>
    function toggleMobileMenu() {
      const nav = document.querySelector('.main-nav');
      nav.classList.toggle('show');
    }
  </script> 