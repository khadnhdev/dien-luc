<!DOCTYPE html>
<html>
<head>
  <title>Đăng nhập - EVN Manager</title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    .login-container {
      min-height: calc(100vh - 200px);
      display: flex;
      align-items: center;
      justify-content: center;
      padding: 2rem 1rem;
    }
    
    .login-card {
      background: white;
      border-radius: 20px;
      box-shadow: 0 10px 30px rgba(0,0,0,.05);
      overflow: hidden;
      width: 100%;
      max-width: 900px;
    }
    
    .login-row {
      display: flex;
      align-items: stretch;
    }
    
    .login-content {
      padding: 3rem;
      flex: 1;
    }
    
    .login-image {
      background: linear-gradient(135deg, var(--primary-color), #0143a3);
      padding: 3rem;
      display: flex;
      flex-direction: column;
      justify-content: center;
      color: white;
      flex: 1;
      position: relative;
      overflow: hidden;
    }
    
    .login-image::before {
      content: '';
      position: absolute;
      top: 50%;
      left: 50%;
      width: 600px;
      height: 600px;
      background: rgba(255,255,255,.1);
      border-radius: 50%;
      transform: translate(-50%, -50%);
    }
    
    .login-title {
      font-size: 2rem;
      font-weight: 700;
      margin-bottom: 1rem;
      position: relative;
    }
    
    .login-subtitle {
      font-size: 1.1rem;
      opacity: 0.9;
      margin-bottom: 2rem;
      position: relative;
    }
    
    .features-list {
      list-style: none;
      padding: 0;
      margin: 0;
      position: relative;
    }
    
    .features-list li {
      display: flex;
      align-items: center;
      gap: 1rem;
      margin-bottom: 1rem;
      font-size: 1.1rem;
    }
    
    .features-list li i {
      width: 32px;
      height: 32px;
      display: flex;
      align-items: center;
      justify-content: center;
      background: rgba(255,255,255,.2);
      border-radius: 8px;
    }
    
    .login-button {
      background: white;
      color: var(--primary-color);
      border: none;
      border-radius: 12px;
      padding: 1rem 2rem;
      font-size: 1.1rem;
      font-weight: 600;
      display: flex;
      align-items: center;
      gap: 1rem;
      transition: all 0.2s;
    }
    
    .login-button:hover {
      transform: translateY(-2px);
      box-shadow: 0 4px 12px rgba(0,0,0,.1);
    }
    
    .login-button i {
      font-size: 1.2rem;
    }
    
    @media (max-width: 768px) {
      .login-row {
        flex-direction: column;
      }
      
      .login-content, .login-image {
        padding: 2rem;
      }
      
      .login-title {
        font-size: 1.75rem;
        text-align: center;
      }
      
      .login-subtitle {
        text-align: center;
      }
      
      .features-list {
        margin-bottom: 2rem;
      }
      
      .login-button {
        width: 100%;
        justify-content: center;
      }
    }
  </style>
</head>
<body>
  <%- include('navbar') %>

  <div class="login-container">
    <div class="login-card">
      <div class="login-row">
        <div class="login-content">
          <h1 class="login-title">Đăng nhập</h1>
          <p class="login-subtitle">
            Đăng nhập để nhận thông báo lịch cúp điện qua email và quản lý đăng ký của bạn
          </p>
          <a href="/auth/google" class="login-button">
            <i class="bi bi-google"></i>
            Đăng nhập với Google
          </a>
        </div>
        <div class="login-image">
          <h2 class="login-title">Tính năng</h2>
          <ul class="features-list">
            <li>
              <i class="bi bi-bell"></i>
              <span>Nhận thông báo qua email khi có lịch cúp điện mới</span>
            </li>
            <li>
              <i class="bi bi-geo-alt"></i>
              <span>Đăng ký theo dõi nhiều khu vực khác nhau</span>
            </li>
            <li>
              <i class="bi bi-clock-history"></i>
              <span>Cập nhật thông tin liên tục từ các công ty điện lực</span>
            </li>
            <li>
              <i class="bi bi-gear"></i>
              <span>Tùy chỉnh cài đặt thông báo theo nhu cầu</span>
            </li>
          </ul>
        </div>
      </div>
    </div>
  </div>

  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html> 